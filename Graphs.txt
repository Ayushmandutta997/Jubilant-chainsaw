------BFS------
ll n,e;
  i2(n,e);
  vvi adj(n);
  lop(e)
  {
   ll u,v;
   i2(u,v);
   adj[u].pb(v);
   adj[v].pb(u);
  }
  queue<ll>q;
  vector<ll>vis(n,0);
  q.push(0);
  while(!q.empty())
  {
   int node=q.front();
   q.pop();
   if(!vis[node])
   {
   cout<<node<<nl;
   vis[node]=1;
   }
   vector<int>:: iterator it; 
   for(it=adj[node].begin();it<=adj[node].end();it++)
   {
    if(!vis[*it]) 
    q.push(*it);
   }
  }
------------------------------------